const A = ["P", "I", "N" , "E", "A", "P", "P", "L" ,"E"]

const reversedArray = []
// REVERSE THE ARRAY

for(let i = A.length -1; i >= A.length -A.length; i--){
    reversedArray.push(A[i])
}

console.log(reversedArray);
